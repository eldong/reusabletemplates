####################################################################
# Demo 3 — Consumer: calls the reusable workflow from eldong/.github
####################################################################
name: "Demo 3: CI (org consumer)"

on:
  workflow_dispatch:

jobs:
  ci:
    uses: eldong/.github/.github/workflows/demo3-reusable-ci.yml@main
    with:
      node-version: "20"
      enable-coverage: true
